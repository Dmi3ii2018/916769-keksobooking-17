'use strict';

(function () {
  //mapCardsContainer.querySelectorAll()

  window.advertListener = {
    onPinClick: function (evt) {
      evt.preventDefault();
      window.advertList.forEach(function (it) {

      });
    }
  };
})();
